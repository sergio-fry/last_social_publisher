<%= simple_form_for(@channel) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :publisher_klass, collection: Rails.configuration.x.publishers %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
